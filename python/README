More complex python scripts like e.g. gdb python code, reusable python
modules and stuff that uses them and so forth.  Or just python stuff
that did not fit anywhere else.  But above all the gdb python stuff.